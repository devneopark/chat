spring:
  sql:
    init:
      mode: always
      schema-locations:
        - init/schema.sql
      data-locations:
        - init/data.sql

logging:
  level:
    org.springframework.r2dbc.core: debug
