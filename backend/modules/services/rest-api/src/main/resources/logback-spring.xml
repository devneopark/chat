<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <property
            name="TRACE_ID_PATTERN"
            value="%replace(%X{traceId}){'^(.+)$','trace-id=$1 '}"/>

    <property
            name="CONSOLE_LOG_PATTERN"
            value="%clr(%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX}){faint} %highlight(%5p){} %clr(${PID:-}){magenta} %clr([%thread]){faint} %clr(:){faint} %clr(${TRACE_ID_PATTERN}){yellow}%msg%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>